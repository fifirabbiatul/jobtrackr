"use client";
import{WorkspacePage}from"@/components/workspace-page";import{useStore}from"@/lib/store";
export default function Page(){const{applications}=useStore();const total=applications.length;const stages=["Wishlist","Applied","HR Screening","Assessment","HR Interview","User Interview","Final Interview","Offering","Hired"];const sources=[...new Set(applications.map(a=>a.source||"Unknown"))];return <WorkspacePage title="Analytics & Insights" eyebrow="Insight dihitung dari data lamaranmu"><div className="grid gap-5 lg:grid-cols-2"><section className="rounded-2xl border bg-white p-6"><h3 className="font-bold">Hiring Funnel</h3>{!total?<Empty/>:<div className="mt-6 space-y-3">{stages.map(s=>{const n=applications.filter(a=>a.current_status===s).length;return <div key={s}><div className="flex justify-between text-xs"><span>{s}</span><b>{n}</b></div><div className="mt-1 h-7 rounded bg-slate-100"><div className="h-full rounded bg-gradient-to-r from-navy to-teal" style={{width:`${Math.max(n/total*100,n?8:0)}%`}}/></div></div>})}</div>}</section><section className="rounded-2xl border bg-white p-6"><h3 className="font-bold">Application Sources</h3>{!total?<Empty/>:<div className="mt-6 space-y-5">{sources.map(s=>{const n=applications.filter(a=>(a.source||"Unknown")===s).length;return <div key={s}><div className="flex justify-between text-sm"><span>{s}</span><b>{Math.round(n/total*100)}%</b></div><div className="mt-2 h-2 rounded bg-slate-100"><div className="h-full rounded bg-teal" style={{width:`${n/total*100}%`}}/></div></div>})}</div>}</section></div></WorkspacePage>}
function Empty(){return <p className="mt-8 text-center text-sm text-slate-400">Add applications to see analytics.</p>}
